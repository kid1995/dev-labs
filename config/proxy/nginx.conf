# AUTO-GENERATED from routes.yaml — do not edit manually
# Regenerate with: ./scripts/setup-dns.sh --regenerate

# Lab Guide
server {
    listen 80;
    server_name docs.labs.local;
    location / {
        proxy_pass http://lab-guide:80;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# SI UI Story Book
server {
    listen 80;
    server_name design-system.si.local;
    location / {
        proxy_pass http://lab-storybook:80;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Gitea
server {
    listen 80;
    server_name git.labs.local;
    client_max_body_size 100m;
    location / {
        proxy_pass http://lab-gitea:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Jenkins
server {
    listen 80;
    server_name jenkins.labs.local;
    location / {
        proxy_pass http://lab-jenkins:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Keycloak
server {
    listen 80;
    server_name keycloak.labs.local;
    location / {
        proxy_pass http://lab-keycloak:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Hint API
server {
    listen 80;
    server_name hint.elpa.local;
    location / {
        proxy_pass http://lab-hint-backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# DLT Manager API
server {
    listen 80;
    server_name dlt-api.elpa.local;
    location / {
        proxy_pass http://lab-dlt-backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# DLT Frontend
server {
    listen 80;
    server_name dlt-fe.elpa.local;
    location / {
        proxy_pass http://lab-dlt-frontend:80;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

